import type { Player } from '../models/player'; import type { Bug } from '../models/bug'; import { logGame } from './gameLogger'; export function damage(attacker: number, defense: number) { const value = Math.max(1, attacker - defense); logGame('COMBAT_DAMAGE', { value }); return value; } export function getEffectiveAttack(player: Player): number { const bonus = player.buffs.filter(b => b.type === 'attack').reduce((sum, b) => sum + b.value, 0); return player.attack + bonus; } export function getEffectiveDefense(player: Player): number { const bonus = player.buffs.filter(b => b.type === 'defense').reduce((sum, b) => sum + b.value, 0); return player.defense + bonus; } export function attackBug(player: Player, bug: Bug) { const effectiveAttack = getEffectiveAttack(player); const value = damage(effectiveAttack, bug.defense); bug.hp -= value; if (bug.hp <= 0) { bug.status = 'dead'; player.exp += 12; logGame('BUG_DEAD', { type: bug.type }); } return value; }
