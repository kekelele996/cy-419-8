import type { Item } from './item'; export interface Buff { type: 'attack' | 'defense'; value: number; remaining: number; } export interface Player { id:string; name:string; hp:number; max_hp:number; mp:number; max_mp:number; level:number; exp:number; attack:number; defense:number; position:{x:number;y:number}; inventory:Item[]; buffs: Buff[] }
